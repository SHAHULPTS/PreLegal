# Copy to .env and fill in real values. .env is git-ignored.

# Signs backend session cookies. Use a long random string in production.
SESSION_SECRET=change-me-to-a-long-random-string

# API key for the LLM provider (used by future product features, not PL-4).
OPENROUTER_API_KEY=
